// Basic test skeleton for FirestoreSignaling. Use the Firestore emulator for
// running tests locally.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('signaling test placeholder', () {
    // TODO: add Firestore emulator-based tests
    expect(1, 1);
  });
}
